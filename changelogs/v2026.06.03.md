# YAAM version v2026.06.03

## Improvements

- Add missing artifacts bundling in `pyinstall.ps1` script
- Change disabled addons suffix from `.disabled` to `_0`.
  - Further parametrization capabilities are now in place.
